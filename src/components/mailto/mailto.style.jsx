import styled from 'styled-components';

export const MailLink = styled.a`
    text-decoration: inherit;
    color: inherit;

    &:visited {
        text-decoration: inherit;
        color: inherit;
    }
`;